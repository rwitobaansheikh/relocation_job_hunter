name: Build & Deploy (Development)

on:
  push:
    branches: [dev]
    paths:
      - "backend/**"
      - "frontend/**"
      - "Dockerfile"
      - "docker-compose.yml"
      - "docker-compose.dev.yml"
      - ".github/workflows/deploy-dev.yml"
  workflow_dispatch:

concurrency:
  group: deploy-dev
  cancel-in-progress: true

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push image
        id: image
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
        run: |
          IMAGE="$REGISTRY/$ECR_REPOSITORY"
          docker build -t "$IMAGE:dev-${{ github.sha }}" -t "$IMAGE:dev-latest" .
          docker push "$IMAGE:dev-${{ github.sha }}"
          docker push "$IMAGE:dev-latest"
          echo "image=$IMAGE:dev-${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Prepare deployment files
        env:
          APP_ENV: ${{ secrets.APP_ENV_DEV }}
          IMAGE: ${{ steps.image.outputs.image }}
        run: |
          mkdir -p deploy_bundle
          cp docker-compose.yml deploy_bundle/docker-compose.yml
          cp docker-compose.dev.yml deploy_bundle/docker-compose.dev.yml
          printf '%s\n' "$APP_ENV" > deploy_bundle/.env
          echo "ECR_IMAGE=$IMAGE" >> deploy_bundle/.env

      - name: Open ephemeral SSH for this runner
        id: ssh_access
        run: |
          set -euo pipefail
          RUNNER_IP=$(curl -fsS https://checkip.amazonaws.com | tr -d '[:space:]')
          SG_ID="${{ secrets.EC2_SECURITY_GROUP_ID }}"
          if [[ -z "$SG_ID" ]]; then
            echo "Missing GitHub secret EC2_SECURITY_GROUP_ID."
            exit 1
          fi
          aws ec2 authorize-security-group-ingress \
            --group-id "$SG_ID" \
            --ip-permissions "IpProtocol=tcp,FromPort=22,ToPort=22,IpRanges=[{CidrIp=${RUNNER_IP}/32,Description=github-actions-dev-${{ github.run_id }}}]" \
            || true
          echo "runner_ip=$RUNNER_IP" >> "$GITHUB_OUTPUT"
          echo "sg_id=$SG_ID" >> "$GITHUB_OUTPUT"
          sleep 5

      - name: Ensure dev directories on EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          command_timeout: 2m
          script: |
            set -euo pipefail
            sudo mkdir -p /opt/app-dev /data-dev/uploads /data-dev/generated
            sudo chown -R ec2-user:ec2-user /opt/app-dev /data-dev

      - name: Copy files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "deploy_bundle/docker-compose.yml,deploy_bundle/docker-compose.dev.yml,deploy_bundle/.env"
          target: "/opt/app-dev"
          strip_components: 1
          timeout: 60s

      - name: Deploy on EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          command_timeout: 15m
          script: |
            set -euo pipefail
            sudo mkdir -p /opt/app-dev /data-dev/uploads /data-dev/generated
            sudo chown -R ec2-user:ec2-user /opt/app-dev /data-dev
            cd /opt/app-dev
            aws ecr get-login-password --region ${{ env.AWS_REGION }} \
              | docker login --username AWS --password-stdin "$(echo "${{ steps.image.outputs.image }}" | cut -d'/' -f1)"
            COMPOSE="-p app-dev -f docker-compose.yml -f docker-compose.dev.yml"
            if grep -qE '^LLM_PROVIDER=ollama' .env; then
              COMPOSE="$COMPOSE --profile ollama"
            fi
            docker compose $COMPOSE pull
            docker compose $COMPOSE up -d --remove-orphans
            docker image prune -f
            if grep -qE '^LLM_PROVIDER=ollama' .env; then
              if ! docker compose $COMPOSE exec -T ollama ollama list 2>/dev/null | grep -q 'llama3.2:1b'; then
                docker compose $COMPOSE exec -T ollama ollama pull llama3.2:1b
              fi
            fi

      - name: Close ephemeral SSH
        if: always() && steps.ssh_access.outputs.sg_id != ''
        run: |
          aws ec2 revoke-security-group-ingress \
            --group-id "${{ steps.ssh_access.outputs.sg_id }}" \
            --ip-permissions "IpProtocol=tcp,FromPort=22,ToPort=22,IpRanges=[{CidrIp=${{ steps.ssh_access.outputs.runner_ip }}/32}]" \
            || true
